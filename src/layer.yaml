includes:
  - layer:basic
  - layer:apt
  - interface:reverseproxy
options:
  apt:
    packages:
      - weechat-curses
      - weechat-scripts
      # - python3-openssl
  # Add additional layers and interfaces here
